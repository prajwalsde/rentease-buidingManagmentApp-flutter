import 'package:flutter/material.dart';

class RentSummaryStripWidget extends StatelessWidget {
  const RentSummaryStripWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
