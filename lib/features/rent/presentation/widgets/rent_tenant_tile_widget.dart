import 'package:flutter/material.dart';

class RentTenantTileWidget extends StatelessWidget {
  const RentTenantTileWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
