import 'package:flutter/material.dart';

class AddRoomSheetWidget extends StatelessWidget {
  const AddRoomSheetWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
