import 'package:flutter/material.dart';

class BillBreakdownWidget extends StatelessWidget {
  const BillBreakdownWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
