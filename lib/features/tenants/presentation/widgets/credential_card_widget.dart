import 'package:flutter/material.dart';

class CredentialCardWidget extends StatelessWidget {
  const CredentialCardWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
