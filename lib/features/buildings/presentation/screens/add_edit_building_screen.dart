import 'package:flutter/material.dart';

class AddEditBuildingScreen extends StatelessWidget {
  const AddEditBuildingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('AddEditBuildingScreen - TODO'),
      ),
    );
  }
}
