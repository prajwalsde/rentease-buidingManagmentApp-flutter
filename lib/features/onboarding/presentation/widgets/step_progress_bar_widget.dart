import 'package:flutter/material.dart';

class StepProgressBarWidget extends StatelessWidget {
  const StepProgressBarWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
