import 'package:flutter/material.dart';

class MeterReadingCardWidget extends StatelessWidget {
  const MeterReadingCardWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
