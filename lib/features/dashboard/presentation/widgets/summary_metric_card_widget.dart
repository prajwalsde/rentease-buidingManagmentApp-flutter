import 'package:flutter/material.dart';

class SummaryMetricCardWidget extends StatelessWidget {
  const SummaryMetricCardWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
