import 'package:flutter/material.dart';

class LiveCalculationWidget extends StatelessWidget {
  const LiveCalculationWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
