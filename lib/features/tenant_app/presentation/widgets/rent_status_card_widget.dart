import 'package:flutter/material.dart';

class RentStatusCardWidget extends StatelessWidget {
  const RentStatusCardWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
