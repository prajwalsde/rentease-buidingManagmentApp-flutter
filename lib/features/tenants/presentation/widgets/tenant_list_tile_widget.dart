import 'package:flutter/material.dart';

class TenantListTileWidget extends StatelessWidget {
  const TenantListTileWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
