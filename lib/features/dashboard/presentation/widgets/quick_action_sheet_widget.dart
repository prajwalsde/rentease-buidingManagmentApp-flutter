import 'package:flutter/material.dart';

class QuickActionSheetWidget extends StatelessWidget {
  const QuickActionSheetWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
