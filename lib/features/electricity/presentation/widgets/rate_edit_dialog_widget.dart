import 'package:flutter/material.dart';

class RateEditDialogWidget extends StatelessWidget {
  const RateEditDialogWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
