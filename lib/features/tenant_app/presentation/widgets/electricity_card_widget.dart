import 'package:flutter/material.dart';

class ElectricityCardWidget extends StatelessWidget {
  const ElectricityCardWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
