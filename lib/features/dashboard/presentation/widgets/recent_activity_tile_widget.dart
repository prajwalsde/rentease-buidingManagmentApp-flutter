import 'package:flutter/material.dart';

class RecentActivityTileWidget extends StatelessWidget {
  const RecentActivityTileWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
